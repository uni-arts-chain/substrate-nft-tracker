# frozen_string_literal: true

RSpec.describe Substrate::Nft::Tracker do
  it "has a version number" do
    expect(Substrate::Nft::Tracker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
